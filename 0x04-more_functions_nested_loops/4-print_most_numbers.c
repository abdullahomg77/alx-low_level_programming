#include "main.h"

/**
 * print_most_numbers - print all number except 2 and 4
 * Return: Always 0
*/

void print_most_numbers(void)
{
	int i
	for (i = 0; i <= 9; i++)
	{
		if (num == 2 || num == 4)
			continune;
		_putchar(i +48);
	}
	_putchar('\n');
}

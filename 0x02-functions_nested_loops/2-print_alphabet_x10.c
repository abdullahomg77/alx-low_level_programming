#include"main.h"

/**
 * print_alphabet_x10 - fuction that will prinnt the alphabet 10 times
 *
*/

void print_alphabet_x10(void)
{
	int lin, ch;

	for (lin = 0; lne <= 9; lin++)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
			_putchar(ch);
		_putchar('\n');
	}
}
